import { generateHtml } from '../../src/utils/htmlGenerator';
import fs from 'fs';
import path from 'path';

const STATIC_HTML_DIR = path.join(process.cwd(), 'static-html');

export const startStaticHTMLGenerator = () => {
    // 静态HTML生成逻辑
    // ...
}; 